import { TimelineDatasource } from './timeline.datasource';

describe('TimelineDatasource', () => {
  it('should create an instance', () => {
    expect(new TimelineDatasource()).toBeTruthy();
  });
});
